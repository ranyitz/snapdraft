## New

- Added Sparkle update integration for in-app update checks in Sparkle-enabled builds.

## Improved

- Made the release workflow automatically bump patch versions and publish update metadata by default.

## Fixed

- Fixed appcast signing in CI so release jobs can sign Sparkle metadata without interactive keychain prompts.
